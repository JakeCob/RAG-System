# RAG System Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# FastAPI Application Settings
# =============================================================================
APP_NAME="ROMA RAG API"
API_INGEST_TOKEN="local-dev-token"
STREAM_CHUNK_PAUSE_MS=0

# =============================================================================
# LLM Configuration (Phase 4 - P4-1)
# =============================================================================

# LLM Provider Selection
# Options: "openai" or "anthropic"
LLM_PROVIDER="openai"

# Model Name
# OpenAI options: gpt-4o, gpt-4o-mini, gpt-4-turbo
# Anthropic options: claude-sonnet-3.5, claude-opus-3, claude-haiku-3
LLM_MODEL="gpt-4o"

# API Keys (Set based on your chosen provider)
OPENAI_API_KEY="sk-your-openai-api-key-here"
ANTHROPIC_API_KEY="sk-ant-your-anthropic-api-key-here"

# LLM Request Configuration
LLM_MAX_RETRIES=3
LLM_TIMEOUT_SECONDS=30
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048

# =============================================================================
# Vector Database Configuration
# =============================================================================
# LanceDB directory (uses local filesystem storage)
# RAG_LANCEDB_PATH="./data/lancedb"

# =============================================================================
# Google Drive Connector (Phase 4 - P4-2)
# =============================================================================
# Path to Google Drive service account JSON credentials file
# To set up:
# 1. Create a service account in Google Cloud Console
# 2. Download the JSON credentials file
# 3. Share target Drive folders with the service account email
# 4. Set the path to the credentials file here
RAG_GDRIVE_CREDENTIALS_PATH="credentials/service-account.json"

# Google Drive API OAuth scopes (comma-separated)
# Default: drive.readonly (read-only access)
# RAG_GDRIVE_SCOPES="https://www.googleapis.com/auth/drive.readonly"

# =============================================================================
# Development Settings
# =============================================================================
# Set to "development" for verbose logging
# RAG_ENVIRONMENT="development"
