"""Guardrails Agent - "The Shield".

Ensures safety and security for both incoming user requests
and outgoing agent responses. Acts as the gatekeeper against
prompt injection, malicious payloads, and PII leakage.

Primary Task: UserRequest -> SafeRequest AND AgentResponse -> SafeResponse
"""
